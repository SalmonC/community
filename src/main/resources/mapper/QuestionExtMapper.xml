<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="salmon.community.mapper.QuestionExtMapper">
    <resultMap id="BaseResultMap" type="salmon.community.model.Question">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Fri Sep 03 04:13:37 CST 2021.
        -->
        <id column="ID" jdbcType="BIGINT" property="id"/>
        <result column="TITLE" jdbcType="VARCHAR" property="title"/>
        <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate"/>
        <result column="GMT_MODIFIED" jdbcType="BIGINT" property="gmtModified"/>
        <result column="CREATOR" jdbcType="BIGINT" property="creator"/>
        <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount"/>
        <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount"/>
        <result column="LIKE_COUNT" jdbcType="INTEGER" property="likeCount"/>
        <result column="TAG" jdbcType="VARCHAR" property="tag"/>
    </resultMap>
    <resultMap extends="BaseResultMap" id="ResultMapWithBLOBs" type="salmon.community.model.Question">
        <result column="DESCRIPTION" jdbcType="CLOB" property="description"/>
    </resultMap>

    <update id="incView" parameterType="salmon.community.model.Question">
        update QUESTION
        set VIEW_COUNT = view_count + #{viewCount,jdbcType=INTEGER}
        where id = #{id}
    </update>
    <update id="incCommentCount" parameterType="salmon.community.model.Question">
        update QUESTION
        set COMMENT_COUNT = COMMENT_COUNT + #{commentCount,jdbcType=INTEGER}
        where id = #{id}
    </update>
    <select id="selectRelated" resultMap="BaseResultMap">
        select *
        from QUESTION
        WHERE id != #{id}
          and tag regexp #{tag};
    </select>
    <select id="countBySearch" parameterType="salmon.community.dto.QuestionQueryDTO" resultType="java.lang.Integer">
        select count(*)
        from QUESTION
        <where>
            <if test="search!=null and search!=''">
                title regexp #{search};
            </if>
        </where>
    </select>
    <select id="selectBySearchWithBLOBs" parameterType="salmon.community.dto.QuestionQueryDTO"
            resultMap="ResultMapWithBLOBs">
        select *
        from QUESTION
        <where>
            <if test="search!=null">
                title regexp #{search}
            </if>
        </where>
        order by gmt_modified desc
        LIMIT #{offset},#{size}
    </select>

    <select id="selectHot" parameterType="salmon.community.dto.QuestionQueryDTO"
            resultMap="ResultMapWithBLOBs">
        select *
        from QUESTION
        order by comment_count desc,
                 view_count desc,
                 gmt_modified desc
        LIMIT 5
    </select>
</mapper>